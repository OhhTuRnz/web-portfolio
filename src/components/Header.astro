---
---

<header class="fixed top-0 w-full bg-slate-900/90 backdrop-blur-md border-b border-slate-800/50 z-50">
  <nav class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="#hero" class="flex items-center space-x-3 group">
        <img 
          src="/logos/carrusk_logo_monkey.jpeg" 
          alt="Carrasco Logo" 
          class="w-8 h-8 rounded-full transition-transform group-hover:scale-110"
        />
        <span class="text-lg font-medium text-white">Alejandro Carrasco</span>
      </a>
      
      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        <a href="#about" class="text-gray-400 hover:text-white transition-colors font-light">About</a>
        <a href="#projects" class="text-gray-400 hover:text-white transition-colors font-light">Projects</a>
        <a href="#publications" class="text-gray-400 hover:text-white transition-colors font-light">Publications</a>
        <a href="#contact" class="text-gray-400 hover:text-white transition-colors font-light">Contact</a>
      </div>
      
      <!-- Mobile menu button -->
      <button 
        id="mobile-menu-button"
        class="md:hidden p-2 rounded-md text-gray-400 hover:text-white hover:bg-slate-800/50 transition-colors"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
    
    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden pb-4">
      <div class="flex flex-col space-y-2">
        <a href="#about" class="text-gray-400 hover:text-white transition-colors py-2 font-light">About</a>
        <a href="#projects" class="text-gray-400 hover:text-white transition-colors py-2 font-light">Projects</a>
        <a href="#publications" class="text-gray-400 hover:text-white transition-colors py-2 font-light">Publications</a>
        <a href="#contact" class="text-gray-400 hover:text-white transition-colors py-2 font-light">Contact</a>
      </div>
    </div>
  </nav>
</header>

<script>
  // Mobile menu toggle
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  
  mobileMenuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
  
  // Close mobile menu when clicking on links
  const mobileLinks = mobileMenu?.querySelectorAll('a');
  mobileLinks?.forEach(link => {
    link.addEventListener('click', () => {
      mobileMenu?.classList.add('hidden');
    });
  });
</script>